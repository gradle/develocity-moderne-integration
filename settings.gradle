plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.6.0"
    id "com.gradle.enterprise" version "3.14.1"
}

rootProject.name = "gradle-build-scan-moderne-integration"
gradleEnterprise {
    server = 'https://ge.example.com/'
    buildScan {
        publishAlways()
    }
}
